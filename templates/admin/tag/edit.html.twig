{% extends 'base.html.twig' %}

{% block title %}Modifier un mot clé{% endblock %}

{% block body %}
    <div class="container admin-videos-container">
        <h1>Modifier un mot clé</h1>

        {{ include('admin/tag/_form.html.twig', {'button_label': 'Modifier'}) }}

        <a href="{{ path('app_tag_index') }}" class="btn-blue back">Revenir à la liste</a>
    </div>
{% endblock %}