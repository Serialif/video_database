{% extends 'base.html.twig' %}

{% block title %}Nouveau mot clé{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('form') }}
{% endblock %}

{% block body %}
    <div class="container form-container">
        <h1>Ajouter un nouveau mot clé</h1>

        {{ include('admin/tag/_form.html.twig', {'button_label': 'Ajouter'}) }}

        <a href="{{ path('app_tag_index') }}" class="btn-blue back">Revenir à la liste</a>
    </div>
{% endblock %}