{{ form_start(form,  {'attr': {'class': 'form-video', 'novalidate': true}}) }}
    {{ form_widget(form) }}
<button class="btn-green form-btn">{{ button_label|default('Ajouter') }}</button>
{{ form_end(form) }}
