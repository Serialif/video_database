<ul class="menu">
    <li><a href="{{ path('app_home') }}" class="menu-item">Accueil</a></li>
    {% if is_granted('ROLE_ADMIN') %}
        <li><a href="{{ path('app_video_index') }}" class="menu-item">Administration</a></li>
    {% endif %}
    <li>
        {% if app.user is null %}
            <a href="{{ path('app_login') }}" class="menu-item">Se connecter</a>
        {% else %}
            <a href="{{ path('app_logout') }}" class="menu-item">Se déconnecter</a>
        {% endif %}
    </li>
</ul>